FoamFile
{
    version     2.0;
    format      ascii;
    class       volScalarField;
    object      nut;
}

dimensions      [0 2 -1 0 0 0 0];
internalField   uniform 0;

boundaryField
{
    tube_inlets  { type calculated; value uniform 0; }
    tube_outlets { type calculated; value uniform 0; }
    ".*_to_.*"  { type nutkWallFunction; value uniform 0; }
    defaultFaces { type calculated; value uniform 0; }
}
