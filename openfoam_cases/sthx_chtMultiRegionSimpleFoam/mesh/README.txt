Place SALOME export here as:
  mesh/STHX_fluid_domain.unv

Required group names in the exported mesh:
  Volume groups: shell_side_fluid, tube_side_fluid
  Face groups: shell_inlet, shell_outlet, tube_inlets, tube_outlets, outer_wall
